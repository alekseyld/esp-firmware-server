ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        watch = [ com.alekseyld.ApplicationKt.module ]
    }
    application {
        modules = [ com.alekseyld.ApplicationKt.module ]
    }
}
